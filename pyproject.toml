[tool.poetry]
name = "machine-setup"
version = "0.1.0"
description = ""
authors = ["Brian Leach <brian@amprobotics.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
ansible = "^9.1.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
